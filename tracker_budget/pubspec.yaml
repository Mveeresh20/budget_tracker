name: budget_tracker
description: A simple budget and expense tracker app.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.5.0
  firebase_auth: ^5.3.2
  cloud_firestore: ^5.4.2

  provider: ^6.0.0
  intl: ^0.19.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/en.json
    - assets/es.json
